<div>
  <h2><%= @title %></h2>

  <.form
    let={f}
    for={@changeset}
    id="bookmark-form"
    phx-target={@myself}
    phx-change="validate"
    phx-submit="save"
  >
    <%= label(f, :url) %>
    <%= text_input(f, :url) %>
    <%= error_tag(f, :url) %>

    <%= label(f, :title) %>
    <%= text_input(f, :title) %>
    <%= error_tag(f, :title) %>

    <%= hidden_input(f, :user_id) %>

    <div>
      <%= submit("Save", phx_disable_with: "Saving...") %>
    </div>
  </.form>
</div>
